# Datasets — place locally, never commit real data
data/**
!data/README.md

# External baseline clones — place locally, never commit
external/**
!external/README.md

# Checkpoints and model weights
checkpoints/**
*.pt
*.pth
*.ckpt

# Python cache
__pycache__/
*.pyc
*.pyo
.Python
*.egg-info/
dist/
build/

# Results — generated locally, not committed
results/**
!results/README.md

# Paper build artifacts — keep source/template files tracked
paper/*.pdf
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.brf
paper/*.fdb_latexmk
paper/*.fls
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.toc
paper/*.lof
paper/*.lot
paper/*.zip
paper/template/*.zip

# OMX planning state — not source of truth
.omx/
.omc/

# OS and editor noise
.DS_Store
*.swp
*.swo
.idea/
.vscode/
